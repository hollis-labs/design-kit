# docs

Repo-level design documentation for `design-kit`.

Expected to land here:

- `token-contract.md` — the token contract: the names and their types, for
  color, type, radius and spacing. Authored by CW-20260910-0111. This is the
  document `packages/design-tokens` implements and everything else depends on.

Package-specific documentation stays with its package.
